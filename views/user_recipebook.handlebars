<h1>User RecipeBooks</h1>

{{#each recipebook}}
	<a href="/recipebook/{{dataValues.id}}">
		<p>{{ dataValues.name }}</p>
	</a>
	{{#if dataValues.private}}
		<p>Private</p>
	{{/if}}
	{{#unless dataValues.private}}
		<p>Public</p>
	{{/unless}}
{{/each}}
<a href="/recipebook/create_new_recipebook">Add new recipebook</a>